{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Items</h1>
    <ul>
        {% for category in categories%}
            <li>{{ category.name }}</li>
        {% endfor %}
    </ul>
{% endblock %}